name: 'First CI Workflow'

on: ['push']

jobs:
  build:
    runs-on: 'ubuntu-latest'
    steps:
      - uses: 'actions/checkout@v1'
      
      - name: 'Say Hello'
        run: echo "Hi from ${{github.workflow}} on ${{github.event_name}} executed by ${{github.actor}}"
